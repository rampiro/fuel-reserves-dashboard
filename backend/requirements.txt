# ============================================================
# requirements.txt — Dashboard de Reservas de Combustible
# ============================================================

# API Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client asíncrono (para llamar a EIA, IEA, CORES)
httpx>=0.27.0

# Validación de modelos
pydantic>=2.7.0

# Variables de entorno
python-dotenv>=1.0.1

# Parsing de Excel de CORES (opcional pero recomendado)
openpyxl>=3.1.2

# Scheduler para actualización automática cada 24h (opcional)
apscheduler>=3.10.4

# Para tests
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0   # también se usa como cliente de test con TestClient
